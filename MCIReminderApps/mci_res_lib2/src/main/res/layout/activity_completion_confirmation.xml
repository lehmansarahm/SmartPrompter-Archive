<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:components="http://schemas.android.com/apk/res-auto"
    tools:context=".activities.CompletionConfirmationActivity">

    <TextView
        android:id="@+id/progressText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="75sp"
        android:gravity="center_horizontal"
        android:textSize="18sp"
        android:visibility="gone"
        android:text="Progress:" />

    <ProgressBar
        android:id="@+id/progressBar"
        style="@android:style/Widget.ProgressBar.Horizontal"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="100sp"
        android:layout_gravity="center_horizontal"
        android:visibility="gone"
        android:progress="25"/>

    <edu.temple.mci_res_lib2.utils.GifImageView
        android:id="@+id/GifImageView"
        android:visibility="gone"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        components:src="@raw/high_five_puppy"/>

    <TextView
        android:id="@+id/confirmationText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center_vertical|center_horizontal"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="200sp"
        android:textSize="34sp"
        android:text="Time to drink water!  Press button below when you are on your way!" />

    <Button
        android:id="@+id/acknowledgeButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="On my way!"
        android:layout_gravity="bottom|center"
        android:textSize="24sp" />

</android.support.design.widget.CoordinatorLayout>